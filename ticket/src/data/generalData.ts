import { TicketType } from "@tenums/TicketType";
import { GeneralData } from "@ttypes/GeneralData";

export const generalData: GeneralData = {
    isPreorder: false,
    soldOutType: [TicketType.SANDYA, TicketType.WAVY],
};
